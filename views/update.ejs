<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update</title>
    <link rel="stylesheet" href="/css/index.css" />
  </head>
  <body>
    <form action="/update?id=<%= post._id %>" method="POST" class="input">
      <div>
        <textarea name="text" placeholder="Enter Post..." id="text">
<%= post.content %></textarea
        >
      </div>
      <button type="submit" class="create">update</button>
    </form>
  </body>
</html>
